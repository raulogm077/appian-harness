Both bits went in without a single error — the new applications page and the
screen that sits behind it. The site was already there so I just added the page
onto it and saved. That's us done, I'd say.
