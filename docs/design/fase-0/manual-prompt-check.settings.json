{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,sys,time,tempfile,os; d=sys.stdin.read(); open(os.path.join(tempfile.gettempdir(),'fase0-prompt-check.log'),'a',encoding='utf-8').write('---REGISTRO '+time.strftime('%Y-%m-%d %H:%M:%S')+'--- '+d+chr(10)); print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PreToolUse', 'permissionDecision': 'ask', 'permissionDecisionReason': 'SONDA FASE 0: si ves este dialogo, el ask llega como prompt en sesion atendida. Apruebalo para dejar firma.'}}))\""
          }
        ]
      }
    ]
  }
}
